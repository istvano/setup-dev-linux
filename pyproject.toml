[project]
name = "linux-os-setup-controller"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = [
  "ansible-core==2.21.4",
  "jsonschema==4.26.0",
]

[dependency-groups]
dev = [
  "ansible-lint==26.8.0",
  "pytest==9.1.1",
]

[tool.uv]
package = false
